# Default calculation properties

# Gridded surface discretization
#   (this also used as floating rupture interval)
GRIDDED_SURFACE_UNIT = 1.0

# How area sources are discretized;
#   (this may be dependent on site-to-source distance)
AREA_SOURCE_SCALING = SCALED_LARGE

# How ruptures float on a source surface
RUPTURE_FLOATING_MODEL = NSHM
